---
triggers: [3817 actionable, golden path spike disposition, onboarding spike issues, what did tv-swim file on 3817, missed inbox replies decomposition]
stale_after: 2026-12-31
---

# Making swimlane4/hearth#3817 canonical and actionable — 2026-09-20

**Order:** `3d06c8cb-2489-4b67-84df-83e06172bda5` · **Conversation:** `78a112ae-5dde-4768-945e-eaa41cc0a325` · **Attempt:** `482b6823-f842-4818-9e0f-4afe12c82a4d` · lane@tv-swim pool worker.

**Everything below carries its command, log line or `file:line`.** Where something could not be established, it says so. Read-only throughout: **no credential was minted**, no code was modified, no PR opened, no BOARD claim posted, and **no lane's acceptance is asserted anywhere** — every owner named is PROPOSED.

All code claims re-verified against the **serving build** `/Users/aaryn/workspaces/swimlane4/hearth` @ `84b15da0e2b7d6ec86136023687356a55290aedb` (`2026-09-20T13:24:32Z`), confirmed with `git log -1 --format='%H %cI'`. API reads against `http://localhost:3000` (port 4000 is a different service and 404s these paths).

---

## 1 · Issues touched and created

| URL | What it covers | Action | Proposed owner | Dependency |
|---|---|---|---|---|
| [hearth#3817](https://github.com/swimlane4/hearth/issues/3817) | The golden-path onboarding spike | **4 comments** — canonical declaration; corrected evidence log; Aaryn's acceptance + 9-step mapping; disposition table | — | — |
| [hearth#3819](https://github.com/swimlane4/hearth/issues/3819) | Coordinator birth pack install fails silently, reason discarded (**A2, re-diagnosed**) | **CREATED** | `lane@hearth-dev` *(PROPOSED)* | **none** |
| [hearth#3820](https://github.com/swimlane4/hearth/issues/3820) | Bind `live_sessions.lane` at coordinator spawn (**A1**) | **CREATED** | `lane@hearth-dev` *(PROPOSED)* | **none blocking**; criterion 3 to be designed with #3807 Track B item 3 |
| [hearth#3563](https://github.com/swimlane4/hearth/issues/3563) | `coord:note --inbox` unbounded, no `--unread` | **COMMENT** — the missed-inbox-replies specimen, decomposed by stage | — | — |
| [hearth#3207](https://github.com/swimlane4/hearth/issues/3207) | Persist/resolve/audit a versioned Soul | **COMMENT** — measured `/soul` 404 (**B4**) | — | — |
| [hearth#2057](https://github.com/swimlane4/hearth/issues/2057) | A new lane silently inherits the wrong repository | **COMMENT** — the non-null variant (**A3**) | — | — |

### Acceptance criteria of the two created issues

**#3819** — (1) a failed install leaves a durable, queryable reason (Conversation id, session id, target root); (2) the coordinator itself can discover its pack is degraded and why; (3) a regression test asserts the reason is retrievable **after the spawn response is discarded**, and fails if the durable write is removed; (4) `grep -c 'session.jsonl' docs/hearth/COORDINATOR.md` > 0 and §0 gains an explicit partial-pack branch; (5) a fresh Conversation yields either a complete six-file pack or a named durable reason.

**#3820** — (1) `live_sessions.lane` populated in the same `POST /api/proxy/start` request, measured on the row; (2) an unbindable lane is named by name in the first turn; (3) one read states **both** bindings side by side; (4) a regression test asserts binding with no `coord:note`/`coord:claim` issued; (5) the specimen re-runs with **zero** hand repairs.

---

## 2 · Already covered — and therefore NOT filed

| Item | Why not filed | Evidence |
|---|---|---|
| **A3** wrong repository | #2057 is the same failure class, OPEN | commented with the variant |
| **B1** new Conversation cannot record durable state | #3122 (EPIC · enrollment), #3597, #3589 open. The mechanism ships (`POST /api/admin/coordinator-op/enrollments {conversation_id, reason}`); what is missing is a **policy decision**, not code | `app/api/admin/coordinator-op/enrollments/route.ts` |
| **B3** credential minting | #2883 open. Resolves automatically if B1 does | — |
| **B4** Soul unreadable | #3207 already requires *"read effective Soul and provenance"* — too small to own separately | commented |
| **Q4a–c** COORDINATOR.md §0 rows | folded into #3819 acceptance criterion 4 | — |
| **B5** HEARTH-OP probe | ⛔ **RETRACTED by C1.** Not a finding; deliberately not filed and not linked | — |
| Secondary frictions (`runtime UNDECLARED`, doc cost, unfiltered `pool:lane --show`, serving-build drift, `gh` silence, unscoped portfolio read) | each too small to own; kept as comments on #3817 | — |

### ⚠️ One open loop I did not close

**B2** (`objective_entry_id` empty 500) is routed by #3817 to *"`lane@master`'s existing 'portfolio ops 500 empty' record"*. **I could not find that record.** `gh issue list --repo swimlane4/hearth --search "portfolio ops 500" --state all --limit 15` returns #500, #3817, #3093, #2860, #3047 — none is it. It may live outside this repo or in a lane's own notes.

**I did not file a third issue**, because a duplicate of a record I merely failed to find is worse than a gap. Reported as an open loop. The one-line fix is already located if it turns out no record exists: `lib/coordinator/portfolioOpArgs.ts:208-209` holds the `uuid` validator; `app/api/master-portfolio/ops/route.ts:126` does not call it.

---

## 3 · The claimed coverage that did not hold up

#3817's body asserts A1/A2/A3/B4 are *"already owned by `lane@hearth-dev` … folding them into #3808 and #3807"*. I read both issues **and all their comments** in full.

```
grep over #3807 + #3808 (body + all comments):
  soul / Soul ............... 0 / 0
  source_repo ............... 0      repoint ... 0      clone ... 0
  context pack .............. 0
  objective_entry_id ........ 0      portfolio.track ... 0
  live_sessions.lane ........ 1  (#3808 only, as the repair BANNER's trigger)
```

- **#3807** is the read-only answer path / lane diagnostics / escalation issue. Its 16:15Z comment slice 2 (*"initial purpose/repository/profile"*) genuinely touches **A3**. Its Track B item 3 is adjacent to A1 but concerns lane *automation/admission* state, not session↔lane binding.
- **#3808** is the shell-restriction issue. Its recommendation (2) makes the repair **banner** role-aware. **The banner is not the binding.**

**Conclusion: A1, A2 and B4 had no owner and no implementation issue.** Two are now filed; one is linked. No acceptance by `lane@hearth-dev` is recorded anywhere I can read — which is exactly why the summary should not have been relied on.

---

## 4 · Aaryn's end-to-end acceptance, mapped to measured state

> name a project and repository -> create exactly one durable Conversation -> bind the correct workspace/repository and coordinator identity -> load role/Soul/capabilities -> register objectives and portfolio -> perform supported reads and dispatch a bounded worker. No credential minting or shell repair by the new coordinator should be necessary.

| Step | State | Evidence |
|---|---|---|
| name a project **and repository** | ⚠️ repository never an input | born record `repo: "/Users/aaryn/workspaces/tv-swim"` |
| **exactly one** durable Conversation | ✅ automatic | `lineage.parent: null`, `children: []` |
| bind correct **repository** | ❌ hand repair | `git clone` + `pool:lane --repoint` |
| bind **coordinator identity** | ❌ hand repair | `live_sessions.lane` was NULL |
| load **role** | ✅ automatic | born record `role: "coordinator"` |
| load **capabilities** (pack) | ❌ failed silently | `.hearth/context/` holds only `system/session.jsonl` |
| load **Soul** | ⛔ impossible | `GET …/soul` → **404 `text/html`**, 9552 bytes |
| **register objectives/portfolio** | ⛔ impossible | **5 enrollments fleet-wide; none is `78a112ae`** |
| **supported reads** | ⚠️ partial | `GET /api/pool/lane?lane=tv-swim` → `403 agent credential no_credential` |
| **dispatch a bounded worker** | ✅ works | 7 owned orders |
| ⛔ no credential minting | ❌ violated | `BOARD.md:32111`, `:32112` — cred `37305c05`, ~4 min, 2 requests, 2 empty 500s |
| ⛔ no shell repair | ❌ violated | both repairs above |

**3 of 9 automatic · 2 hand repairs · 2 impossible · 1 partial · both prohibitions violated.**

### Two corrections to #3817's own body, both material

**(a) `state_entries: 0` is no longer true — and the finding gets worse, not milder.** Measured `2026-09-20T19:40:23.980Z`: **8 entries** (4 `decision`, 4 `reference`), **all `epistemic_class: system_observation`** — order acceptances and pool artifacts written by the platform. **The coordinator wrote none, and not one is an objective.** Accurate statement: *the table is written freely by the platform's own settlement path, and the coordinator whose Conversation it describes still cannot put one objective in it.*

**(b) "#2468 was never wired" is wrong.** `app/api/proxy/start/route.ts:17` imports `installCoordinatorContextDir`; `:645` calls it. **The install ran and returned `installed: false`** — proven structurally: `route.ts:680` gates the fallback chain writer on `if (!context?.installed)`, and the lone `session.jsonl` (one `chain_header` + one `born`, no duplicate) can only have come from that fallback. The reason is computed (`coordinatorContext.ts:654`, `:664-668`; `route.ts:659`) and **returned only in the HTTP response at `route.ts:769`, persisted nowhere**. Whether it threw or `writeRevision` refused is **NOT ESTABLISHED and cannot be from outside**. This is #3819.

---

## 5 · The missed-inbox-replies specimen, decomposed

Attached to **#3563**, whose body predicts this exact behaviour: *"it sees the newest note and is deaf to everything else."* No fresh investigation opened.

Method: read-only `store.listCoordEvents` over the pg ledger, the adapter-selection pattern of `scripts/ops/note-consumption.mjs` (which *"has no code path here that can mutate the ledger"*, per its own header).

- **DELIVERY — ✅ verified.** **8** `note.posted`, `actor=lane@tv-swim`, `resource=lane:master`, earliest `18:00:10.915Z`, latest `19:38:57.811Z`. *(Corrects the incident's own "5, earliest ~18:45Z" — that came from the 60-row rolling window of `GET /api/coord?resource=lane:master`. The undercount is itself #3563's subject.)*
- **RETRIEVAL — ⚠️ not measurable.** `note.delivered` is **0 of 8** — but also **0 of 23** notes to `lane:master` in that window from *every* sender, including ones Master demonstrably consumed. Fleet-wide: **237 `note.delivered` vs 12,988 `note.posted`.** **The retrieval stage is uninstrumented**; its absence proves nothing. *This is the correction — an earlier framing verified delivery only and then wrote "unread", asserting a retrieval failure never measured.*
- **ACKNOWLEDGEMENT / CONSUMPTION — ✅ instrumented and decisive.** `note.consumed` is a real durable marker (1,766 fleet-wide). `lane@master` wrote **610 all-time**, earliest `2026-09-17T16:38:07.469Z`, latest `2026-09-20T19:41:17.398Z`. **1 of 8** tv-swim notes consumed — `013f08ac`, posted `19:38:57.811Z`, consumed `19:41:15.946Z`, disposition `(none)`: **the newest one**, 2m18s later.
- **FILTERING — ⭐ the discriminator.** Notes to `lane:master` in `[18:00Z, 19:25Z]`: `lane@hearth-dev` 10 posted / **5 consumed**; `lane@tv-swim` 7 / **0**; `lane@conversation-revamp` 5 / **0**; `lane@pools-52909de3` 1 / **0**. **Master was consuming notes during the window it said it had had no reply — just not these.** Rules out a dead instrument, a dead session, and a global retrieval outage.

**Verdict: the failure is after DELIVERY and before CONSUMPTION, and it is SELECTIVE.** Whether the selection happened in the **query** (recency-scoped read) or the **display** (settled fold / harness truncation) is **not distinguishable from outside Master's session** — both produce this signature, and Master's own transcript is unreadable to this lane. Honest label: *retrieval-or-filtering, selective*. **Not "unread."**

**Recurrence:** `lane@master` recorded this failure against itself seven days earlier — `~/ai-context/coord/LESSONS.md` `2026-09-13T20:08Z`, with the rule *"read `--inbox` UNFILTERED for that lane's actor, never a recency grep."* The lesson existed and did not prevent the repeat.

**Observable facts stated as fact:** Master asked the same question **four times** and at 19:21Z described three orders as *"awaiting settlement"* after all three were **accepted** (18:35:39.715Z, ~19:00Z, ~19:21Z); all three **did** appear in tv-swim's own owned-work blocks (18:34:02.401Z, 18:49:02.559Z, 19:19:02.650Z). **DUPLICATION, not misdelivery — CONFIRMED for `78a112ae`, not presumed.** Nothing was stranded.

---

## 6 · What this deliverable does NOT establish

Recorded explicitly, because this lane has twice been corrected for claiming more than the evidence supported.

1. **Why the pack install failed.** Only *that* it did. The reason is unrecoverable by construction — that is #3819's premise.
2. **Whether Master's inbox failed at retrieval or at filtering.** Not distinguishable from outside its session.
3. **`live_sessions.lane` being NULL at birth was not re-measured retrospectively** — it now reads `"tv-swim"`. It rests on the friction log and on the fact that a repair was required. Stated as reported-and-repaired.
4. **Whether `lane@hearth-dev` or `lane@master` will accept #3819 / #3820.** Neither has been asked by me and **neither has agreed.** Every owner is PROPOSED.
5. **Whether `lane@master`'s "portfolio ops 500 empty" record exists.** I could not find it; B2 remains an open loop.
6. **Whether a scanner gap exists behind B5.** Retracted, unestablished, deliberately not pursued.

---

## 7 · Commands used

```bash
gh issue list --repo swimlane4/hearth --search "<term>" --state all --limit 50   # x7, decomposed
gh issue view {3817,3807,3808,3563,3207,2057,2726,3725,3597,3589,2480,1322} --repo swimlane4/hearth --json body,comments
curl -s http://localhost:3000/api/conversations/78a112ae-…            # 200 application/json
curl -s http://localhost:3000/api/conversations/78a112ae-…/soul       # 404 text/html  9552 bytes
curl -s "http://localhost:3000/api/pool/lane?lane=tv-swim"            # 403 no_credential — LEFT AS A REFUSAL
npm run pool:lane -- --show                                            # 675 lines
npm run ops:note-consumption
node --import tsx /tmp/probe_notes.mjs   # read-only listCoordEvents; per-note receipts + positive control
```

⚠️ The `403` was **left as a refusal and not routed around**. Minting a credential to read a lane row is the exact anti-pattern #3817 §B3 documents, and doing it to investigate #3817 would have made the specimen worse.

---

**Written by a `lane@tv-swim` pool worker, 2026-09-20. No code changed, no PR opened, no acceptance recorded on any lane's behalf.**
